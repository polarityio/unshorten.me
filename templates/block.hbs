<div class="d-flex align-items-center justify-content-end copy-btn-container">
  <button class="btn copy-btn p-action" {{action "copyData"}} title="Copy defanged URL to clipboard">
    {{fa-icon icon="clipboard" fixedWidth=true}}
  </button>
  <div class="copy-success-message {{if showCopyMessage "visible" "hidden"}}">
    {{fa-icon icon="check" fixedWidth=true class="copy-success-icon"}}
    Copied defanged URL to Clipboard.
  </div>
</div>
<div>
  <span class="p-key">Resolved URL:</span>
  <div class="p-block">{{details.resolved_url}}</div>
</div>
<div class="mt-2">
  <span class="p-key">Usage Count:</span>
  <span class="p-value">{{details.usage_count}}</span>
</div>
<div>
  <span class="p-key">Remaining Calls:</span>
  <span class="p-value">{{details.remaining_calls}}</span>
</div>